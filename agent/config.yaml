# ML Performance Survey - Orchestrator Configuration
# Reloaded at the start of each cycle

cycleIntervalMs: 1200000   # 20 minutes between cycles
agentTimeoutMs: 900000     # 15 minutes max per agent
model: claude-opus-4-5
trackerIssue: 1

# Manager agents (run periodically, manage the system)
athenaCycleInterval: 1     # Strategist every cycle
apolloCycleInterval: 1     # HR every cycle

# Worker agents are discovered from agent/workers/ folder
